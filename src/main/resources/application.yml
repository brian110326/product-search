spring:
  datasource:
    url: jdbc:mysql://localhost:3306/esDB
    username: root
    password: brian981103
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  elasticsearch:
    uris: http://localhost:9200

  kafka:
    consumer:
      bootstrap-servers: localhost:9092
    producer:
      bootstrap-servers: localhost:9092

logging:
  level:
    org.elasticsearch.client: TRACE